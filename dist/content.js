// Generated by npm run build; edit src/ instead.
(() => {
  const context = globalThis                                                      ;
  if (context.__adaegisContent) return;
  context.__adaegisContent = true;
  const generic = [
    "ins.adsbygoogle",
    'iframe[src^="https://googleads.g.doubleclick.net/"]',
    'iframe[src^="https://tpc.googlesyndication.com/"]'
  ];
  const youtube = ["ytd-ad-slot-renderer", "ytd-in-feed-ad-layout-renderer",
    "ytd-promoted-sparkles-web-renderer", "ytd-promoted-video-renderer",
    "ytd-display-ad-renderer", "ytd-banner-promo-renderer", "ytd-action-companion-ad-renderer"];
  const isYoutube = location.protocol === "https:" && !location.port &&
    ["www.youtube.com", "m.youtube.com", "youtube.com"].includes(location.hostname);
  const style = document.createElement("style");
  style.dataset.adaegis = "cosmetic";
  style.textContent = [...generic, ...(isYoutube ? youtube : [])].join(",\n") + "{display:none!important}";
  let active = false;
  let timer                    ;
  let resets = 0;
  let refreshing = false;
  let refreshAgain = false;
  function stop()       {
    active = false;
    observer.disconnect();
    if (timer !== undefined) clearTimeout(timer);
    timer = undefined;
    style.remove();
    if (isYoutube) window.dispatchEvent(new Event("adaegis:youtube-stop"));
  }
  function ensureStyle()       {
    timer = undefined;
    if (!active || !document.documentElement || style.isConnected) return;
    if (++resets > 10) { stop(); return; } // Do not fight a page that keeps removing our CSS.
    document.documentElement.append(style);
  }
  const observer = new MutationObserver(() => {
    if (!style.isConnected && active && timer === undefined) timer = window.setTimeout(ensureStyle, 250);
  });
  async function refresh()                {
    if (refreshing) { refreshAgain = true; return; }
    refreshing = true;
    try {
      const policy = await chrome.runtime.sendMessage({ type: "page-policy" });
      if (policy?.ok !== true || typeof policy.cosmetic !== "boolean" || typeof policy.youtube !== "boolean") {
        stop(); return;
      }
      active = policy.cosmetic;
      observer.disconnect();
      if (active) {
        ensureStyle();
        // Only watch the parent of our style, not every change throughout the page.
        if (active) observer.observe(document.documentElement ?? document, { childList: true });
      } else {
        if (timer !== undefined) clearTimeout(timer);
        timer = undefined;
        style.remove();
      }
      if (isYoutube && !policy.youtube) window.dispatchEvent(new Event("adaegis:youtube-stop"));
    } catch { stop(); }
    finally {
      refreshing = false;
      if (refreshAgain) { refreshAgain = false; void refresh(); }
    }
  }
  chrome.runtime.onMessage.addListener((message         , sender) => {
    if (sender.id === chrome.runtime.id && message && typeof message === "object" &&
        (message                      ).type === "refresh-policy") void refresh();
  });
  window.addEventListener("pagehide", stop, { once: true });
  window.addEventListener("pageshow", event => { if (event.persisted) void refresh(); });
  void refresh();
})();
