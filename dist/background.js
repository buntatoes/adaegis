// Generated by npm run build; edit src/ instead.
import { normalize, hostname, exceptionRules,               } from "./settings.js";

const SCRIPT_ID = "youtube-experiment";
let queue                   = Promise.resolve();
function serialize   (operation                  )             {
  const result = queue.then(operation);
  queue = result.catch(() => undefined);
  return result;
}
async function read()                    {
  return normalize(await chrome.storage.local.get(null));
}
async function apply(settings          )                {
  const dnr = chrome.declarativeNetRequest;
  const current = await dnr.getDynamicRules();
  await dnr.updateDynamicRules({
    removeRuleIds: current.filter(rule => rule.id >= 10000 && rule.id < 10200).map(rule => rule.id),
    addRules: exceptionRules(settings.allowlist)
  });
  await dnr.updateEnabledRulesets({
    enableRulesetIds: settings.enabled ? ["core"] : [],
    disableRulesetIds: settings.enabled ? [] : ["core"]
  });
  const scripts = await chrome.scripting.getRegisteredContentScripts({ ids: [SCRIPT_ID] });
  const shouldRegister = settings.enabled && settings.youtubeExperimental;
  if (shouldRegister) {
    const script                                           = {
      id: SCRIPT_ID,
      matches: ["https://www.youtube.com/*", "https://m.youtube.com/*", "https://youtube.com/*"],
      excludeMatches: settings.allowlist.map(host => "https://" + host + "/*"),
      js: ["dist/youtube.js"],
      runAt: "document_start",
      world: chrome.scripting.ExecutionWorld.MAIN,
      persistAcrossSessions: true
    };
    if (scripts.length) await chrome.scripting.updateContentScripts([script]);
    else await chrome.scripting.registerContentScripts([script]);
  } else if (scripts.length) {
    await chrome.scripting.unregisterContentScripts({ ids: [SCRIPT_ID] });
  }
  await dnr.setExtensionActionOptions({ displayActionCountAsBadgeText: settings.enabled });
  await chrome.action.setBadgeText({ text: settings.enabled ? "" : "OFF" });
  await chrome.action.setBadgeBackgroundColor({ color: settings.enabled ? "#126c54" : "#b42318" });
}

async function change(update                                  )                    {
  const previous = await read();
  const next = update(previous);
  try {
    await apply(next);
    await chrome.storage.local.set(next);
    return next;
  } catch (error) {
    // Do not report a saved setting if an extension API rejects part of the change.
    await apply(previous).catch(console.error);
    throw error;
  }
}
const initialize = () => serialize(async () => {
  const settings = await read();
  await apply(settings);
  await chrome.storage.local.set(settings);
});
chrome.runtime.onInstalled.addListener(() => { void initialize().catch(console.error); });
chrome.runtime.onStartup.addListener(() => { void initialize().catch(console.error); });

chrome.runtime.onMessage.addListener((message         , sender, reply) => {
  // Only the packaged popup may mutate extension settings; no page-to-worker bridge.
  if (sender.id !== chrome.runtime.id || sender.url !== chrome.runtime.getURL("popup.html")) return;
  if (!message || typeof message !== "object") return;
  const request = message                           ;
  const task = serialize(async () => {
    if (request.type === "get-state") return read();
    if (request.type === "set-option") {
      const key = request.key;
      if (!["enabled", "cosmetic", "youtubeExperimental"].includes(String(key)) ||
          typeof request.value !== "boolean") throw new Error("Invalid setting.");
      return change(previous => ({ ...previous, [key          ]: request.value }));
    }
    if (request.type === "set-site" && typeof request.allowed === "boolean" &&
        typeof request.tabId === "number") {
      const tab = await chrome.tabs.get(request.tabId);
      const host = hostname(tab.url);
      if (!host || /[/:]/.test(host)) throw new Error("This page does not support site exceptions.");
      return change(previous => {
        const hosts = new Set(previous.allowlist);
        if (request.allowed) hosts.add(host); else hosts.delete(host);
        if (hosts.size > 200) throw new Error("Maximum 200 site exceptions.");
        return { ...previous, allowlist: [...hosts].sort() };
      });
    }
    throw new Error("Unknown request.");
  });
  task.then(settings => reply({ ok: true, settings }),
    error => reply({ ok: false, error: error instanceof Error ? error.message : "Settings update failed." }));
  return true;
});
